name: Call Canonical FUB GHL Superpowers Deploy

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/call-fub-ghl-superpowers-deploy.yml'
  workflow_dispatch:

# Superpowers deployment trigger: 2026-09-01
jobs:
  deploy:
    uses: lehr007-coder/mcp-workers/.github/workflows/deploy-fub-ghl-superpowers-reusable.yml@main
    secrets:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
