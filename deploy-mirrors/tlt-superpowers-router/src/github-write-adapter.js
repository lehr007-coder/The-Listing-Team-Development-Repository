// GENERATED DEPLOYMENT MIRROR. Canonical source is private marketing-superpowers runtime.
export function githubWriteAdapter(env){const service=env?.GITHUB_WRITE,token=env?.GITHUB_WRITE_INTERNAL_TOKEN;if(!service||!token)return null;const execute=(operation,target,args={})=>service.executeApprovedWrite(token,operation,target,args);const read=(operation,target,result)=>service.readBackApprovedWrite(token,operation,target,result);return{createIssue:({repository,title,body})=>execute('github.create_issue',{repository},{title,body}),createBranch:({repository,branch,from_ref})=>execute('github.create_branch',{repository,branch,from_ref},{}),createPullRequest:({repository,head,base,title,body})=>execute('github.create_pull_request',{repository,head,base},{title,body}),createFile:({repository,branch,path,content,message})=>execute('github.create_file_branch',{repository,branch,path},{content,message}),updateFile:({repository,branch,path,content,message,sha})=>execute('github.update_file_branch',{repository,branch,path},{content,message,sha}),getIssue:(repository,number)=>read('github.create_issue',{repository},{number}),getBranch:(repository,branch)=>read('github.create_branch',{repository,branch},{branch}),getPullRequest:(repository,number)=>read('github.create_pull_request',{repository},{number}),getFile:(repository,path,branch)=>read('github.create_file_branch',{repository,path,branch},{path,branch})};}
